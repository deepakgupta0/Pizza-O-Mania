import ProductsList from "./ProductsList"

const Products = () => {
    // const { name } = useContext(CartContext);
    // console.log("Products Page called", name);
    return (
        <>
            <ProductsList></ProductsList>
        </>
    )
}

export default Products
